# NPR Environment Folders
# These override or extend shared folders for NPR
# Use this for environment-specific folders

folders:
  # NPR-specific: Debug folder (only in NPR)
  - name: "Debug & Testing"
    uid: "debug"
    org: "Main Organization"

  - name: "Development Experiments"
    uid: "dev-experiments"
    org: "Development"

  # Override shared folder with NPR-specific settings
  # - name: "Infrastructure (NPR)"
  #   uid: "infrastructure"  # Same uid = overrides shared
  #   org: "Main Organization"
