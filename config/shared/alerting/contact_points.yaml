# =============================================================================
# CONTACT POINTS — Shared across all environments
# =============================================================================
# Uses Grafana's native contact point format with receivers array.
# Environment-specific overrides go in config/<env>/alerting/contact_points.yaml.
#
# Supported receiver types: email, webhook, slack, pagerduty, opsgenie,
#   discord, telegram, teams, googlechat, victorops, pushover, sns,
#   sensugo, threema, webex, line, kafka, oncall, alertmanager
# =============================================================================

contactPoints: []
  # --- Example: Email contact point ---
  # - name: "email-alerts"
  #   org: "Main Organization"
  #   receivers:
  #     - type: "email"
  #       settings:
  #         addresses: "oncall@example.com;team@example.com"
  #         singleEmail: false
  #         subject: "[Grafana Alert] {{ .CommonLabels.alertname }}"
  #       disableResolveMessage: false
  #
  # --- Example: Slack contact point ---
  # - name: "slack-notifications"
  #   org: "Main Organization"
  #   receivers:
  #     - type: "slack"
  #       settings:
  #         url: "https://hooks.slack.com/services/XXXX/YYYY/ZZZZ"
  #         recipient: "#alerts"
  #         title: "{{ .CommonLabels.alertname }}"
  #         text: "{{ .CommonAnnotations.summary }}"
  #       disableResolveMessage: false
  #
  # --- Example: Webhook contact point ---
  # - name: "webhook-alerts"
  #   org: "Main Organization"
  #   receivers:
  #     - type: "webhook"
  #       settings:
  #         url: "https://my-api.example.com/alerts"
  #         httpMethod: "POST"
  #         # authorization_credentials: "vault:webhook-myenv"  # Fetch token from Vault
  #       disableResolveMessage: false
  #
  # --- Example: PagerDuty contact point ---
  # - name: "pagerduty-critical"
  #   org: "Main Organization"
  #   receivers:
  #     - type: "pagerduty"
  #       settings:
  #         integrationKey: "your-pagerduty-integration-key"
  #         severity: "critical"
  #       disableResolveMessage: false
