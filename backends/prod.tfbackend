# Backend configuration for Production environment
# Use with: terraform init -backend-config=backends/prod.tfbackend

# S3 Backend Example
bucket         = "grafana-terraform-state"
key            = "prod/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "grafana-terraform-locks"
