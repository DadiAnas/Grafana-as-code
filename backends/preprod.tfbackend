# Backend configuration for PreProd environment
# Use with: terraform init -backend-config=backends/preprod.tfbackend

# S3 Backend Example
bucket         = "grafana-terraform-state"
key            = "preprod/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "grafana-terraform-locks"
