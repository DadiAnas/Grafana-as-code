# =============================================================================
# SERVICE ACCOUNTS — Shared across all environments
# =============================================================================
# Service accounts provide programmatic access to Grafana.
# Each account is scoped to an organization and assigned a role.
#
# Tokens are auto-generated and can be used for API access.
# Token values are stored in Terraform state (sensitive).
# =============================================================================

service_accounts: []
  # --- Example: Admin service account for automation ---
  # - name: "terraform-automation"
  #   org: "Main Organization"
  #   role: "Admin"               # Admin | Editor | Viewer
  #   is_disabled: false
  #   tokens:
  #     - name: "main-token"
  #       seconds_to_live: 0      # 0 = never expires
  #
  # --- Example: Read-only CI/CD account ---
  # - name: "ci-cd-readonly"
  #   org: "Main Organization"
  #   role: "Viewer"
  #   is_disabled: false
  #   tokens:
  #     - name: "pipeline-token"
  #       seconds_to_live: 31536000  # 1 year in seconds
  #
  # --- Example: Dashboard provisioner ---
  # - name: "dashboard-sync"
  #   org: "Main Organization"
  #   role: "Editor"
  #   is_disabled: false
  #   tokens:
  #     - name: "sync-token"
  #       seconds_to_live: 0
